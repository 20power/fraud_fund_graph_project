# 数据目录说明

项目采用单文件夹自包含结构。甲方原始文件保存在 `data/raw/`，配置使用相对于项目根目录的路径；移动整个项目文件夹后无需修改盘符或绝对路径。

- `raw/`：账户表、交易边表、风险标签表和数据说明；流水线只读使用；
- `interim/`：标准化和缓存的中间数据；
- `processed/`：特征、划分和模型输入；
- `samples/`：用于测试和演示的最小脱敏样例。

实际输入路径由 `configs/data_config.yaml` 控制。
